## Suggestions and notes — Playground

- Worker sandbox
  - Move compile/eval off the main thread; add an abortable timeout.
- Snapshots
  - Provide one-click snapshot of JSX → IR to help author reproducible bug reports.
