## Suggestions and notes — Tool: Agent

- Command packs
  - Group common flows (check → build → test) under a single command with progress UI.
- Output
  - Stream Deno stdio into VS Code Output channel with clear headings and timing.
