import type { CurrencyCode } from "@sitebender/toolsmith/types/branded/index.ts"

//++ Extracts the underlying string value from a CurrencyCode
export default function unwrapCurrencyCode(currencyCode: CurrencyCode): string {
	return currencyCode
}
