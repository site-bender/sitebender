import type { PhoneNumber } from "@sitebender/toolsmith/types/branded/index.ts"

//++ Extracts the underlying string value from a PhoneNumber
export default function unwrapPhoneNumber(phoneNumber: PhoneNumber): string {
	return phoneNumber
}
