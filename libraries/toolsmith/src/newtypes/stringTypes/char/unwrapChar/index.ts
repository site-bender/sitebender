import type { Char } from "@sitebender/toolsmith/types/branded/index.ts"

//++ Extracts the underlying string value from a Char
export default function unwrapChar(char: Char): string {
	return char
}
