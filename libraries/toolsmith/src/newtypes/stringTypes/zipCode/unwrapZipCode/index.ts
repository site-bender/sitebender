import type { ZipCode } from "@sitebender/toolsmith/types/branded/index.ts"

//++ Extracts the underlying string value from a ZipCode
export default function unwrapZipCode(zipCode: ZipCode): string {
	return zipCode
}
