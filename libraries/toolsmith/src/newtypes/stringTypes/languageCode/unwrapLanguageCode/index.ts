import type { LanguageCode } from "@sitebender/toolsmith/types/branded/index.ts"

//++ Extracts the underlying string value from a LanguageCode
export default function unwrapLanguageCode(languageCode: LanguageCode): string {
	return languageCode
}
