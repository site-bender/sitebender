{
	// Engine UI library - Reactive calculations and progressive enhancement
	"name": "@sitebender/engine",
	"version": "0.1.0",
	"description": "A library for engine user interfaces with reactive calculations and progressive enhancement.",

	// Main export point for the library
	"exports": {
		".": "./src/index.ts"
	},

	"tasks": {
		// Run tests - all behavioral tests together
		"name": "@sitebender/engine",
		"description": "Declarative behavior engine for engine UIs with progressive enhancement.",
		"test:cov": "deno test --unstable-temporal --coverage=coverage tests/ --allow-read && deno coverage coverage && deno coverage coverage --lcov > coverage.lcov",

		// Code quality (inherits config from root workspace)
		"lint": "deno lint src/",
		"fmt": "deno fmt src/",
		"type-check": "deno check --unstable-temporal src/**/*.ts src/**/*.tsx"
	},

	// Test configuration
	"test": {
		"include": ["tests/"],
		"exclude": ["src/", "tests/helpers/"]
	}
}
