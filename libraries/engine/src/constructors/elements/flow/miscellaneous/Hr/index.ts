import GlobalEmpty from "../../../../../constructors/abstracted/GlobalEmpty/index.ts"

/**
 * Creates an Hr element configuration object
 *
 * The hr element represents a paragraph-level thematic break,
 * such as a scene change in a story or a transition to another topic.
 * It renders as a horizontal rule.
 *
 * @example
 * ```typescript
 * const hr = Hr({ id: "section-break", class: "divider" })
 * ```
 */
export const Hr = GlobalEmpty("Hr")

export default Hr
