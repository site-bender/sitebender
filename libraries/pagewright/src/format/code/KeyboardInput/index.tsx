/**
 * KeyboardInput component
 *
 * Renders keyboard input using the kbd element. Used to indicate
 * keys to press or keyboard shortcuts. Can handle key combinations.
 *
 * Example usage:
 *
 * Press <KeyboardInput>Ctrl</KeyboardInput>+<KeyboardInput>C</KeyboardInput> to copy.
 *
 * <KeyboardInput combo>Cmd+Shift+P</KeyboardInput> opens the command palette.
 */
