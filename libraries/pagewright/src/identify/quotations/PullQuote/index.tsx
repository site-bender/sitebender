/**
 * PullQuote component
 *
 * Renders an emphasized quotation pulled from the main text,
 * typically displayed larger or in a callout box. Uses blockquote
 * with specific styling classes. Common in magazine/article layouts.
 *
 * Example usage:
 *
 * <PullQuote>
 *   The most important feature we shipped wasn't a feature at all—it
 *   was the decision to listen to our users.
 * </PullQuote>
 */
