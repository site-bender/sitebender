/**
 * SkipLinkRegion component
 *
 * Wraps multiple SkipLink components to provide a region that can
 * contain multiple skip links. This is useful for grouping skip links
 * to show and hide them together.
 *
 * Example usage:
 *
 * <SkipLinkRegion>
 *   <SkipLink href="#main-content">Skip to main content</SkipLink>
 *   <SkipLink href="#footer">Skip to footer</SkipLink>
 * </SkipLinkRegion>
 */
