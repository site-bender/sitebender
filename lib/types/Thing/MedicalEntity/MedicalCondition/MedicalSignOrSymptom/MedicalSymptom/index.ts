import MedicalSignOrSymptom from "../index.ts"

// MedicalSymptom extends MedicalSignOrSymptom but adds no additional properties

export default interface MedicalSymptom extends MedicalSignOrSymptom {
}
