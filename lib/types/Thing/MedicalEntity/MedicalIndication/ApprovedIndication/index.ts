import type MedicalIndication from "../index.ts"

// ApprovedIndication extends MedicalIndication but adds no additional properties

export default interface ApprovedIndication extends MedicalIndication {
}
