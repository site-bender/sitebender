import StatusEnumeration from "../index.ts"

// ReservationStatusType extends StatusEnumeration but adds no additional properties

export default interface ReservationStatusType extends StatusEnumeration {
}
