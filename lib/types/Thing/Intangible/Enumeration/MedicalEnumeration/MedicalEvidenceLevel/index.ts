import MedicalEnumeration from "../index.ts"

// MedicalEvidenceLevel extends MedicalEnumeration but adds no additional properties

export default interface MedicalEvidenceLevel extends MedicalEnumeration {
}
