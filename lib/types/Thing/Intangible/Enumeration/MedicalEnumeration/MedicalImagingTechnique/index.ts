import type MedicalEnumeration from "../index.ts"

// MedicalImagingTechnique extends MedicalEnumeration but adds no additional properties

export default interface MedicalImagingTechnique extends MedicalEnumeration {
}
