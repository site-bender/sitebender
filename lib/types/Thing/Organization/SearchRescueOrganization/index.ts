import type Organization from "../index.ts"

// SearchRescueOrganization extends Organization but adds no additional properties

export default interface SearchRescueOrganization extends Organization {
}
