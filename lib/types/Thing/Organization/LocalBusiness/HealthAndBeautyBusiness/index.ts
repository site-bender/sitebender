import type LocalBusiness from "../index.ts"

// HealthAndBeautyBusiness extends LocalBusiness but adds no additional properties

export default interface HealthAndBeautyBusiness extends LocalBusiness {
}
