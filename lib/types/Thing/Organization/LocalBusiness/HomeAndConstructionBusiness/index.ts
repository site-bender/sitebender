import type LocalBusiness from "../index.ts"

// HomeAndConstructionBusiness extends LocalBusiness but adds no additional properties

export default interface HomeAndConstructionBusiness extends LocalBusiness {
}
