import type LocalBusiness from "../index.ts"

// TouristInformationCenter extends LocalBusiness but adds no additional properties

export default interface TouristInformationCenter extends LocalBusiness {
}
