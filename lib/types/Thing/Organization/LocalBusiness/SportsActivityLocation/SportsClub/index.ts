import SportsActivityLocation from "../index.ts"

// SportsClub extends SportsActivityLocation but adds no additional properties

export default interface SportsClub extends SportsActivityLocation {
}
