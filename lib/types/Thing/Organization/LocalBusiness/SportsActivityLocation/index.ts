import type LocalBusiness from "../index.ts"

// SportsActivityLocation extends LocalBusiness but adds no additional properties

export default interface SportsActivityLocation extends LocalBusiness {
}
