import SportsActivityLocation from "../index.ts"

// GolfCourse extends SportsActivityLocation but adds no additional properties

export default interface GolfCourse extends SportsActivityLocation {
}
