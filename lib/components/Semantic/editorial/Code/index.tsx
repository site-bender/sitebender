/**
 * Code component
 *
 * Renders inline code using the code element. For code blocks,
 * wrap in a pre element. Supports language hints for syntax
 * highlighting via data attributes.
 *
 * Example usage:
 *
 * Use the <Code>useState</Code> hook for state management.
 *
 * <Code lang="javascript">
 *   const result = items.filter(x => x > 0)
 * </Code>
 */