import GlobalEmpty from "../../../../../constructors/abstracted/GlobalEmpty/index.ts"

/**
 * Creates a Br element configuration object
 *
 * The br element represents a line break.
 * It is a void element and cannot contain children.
 *
 * @example
 * ```typescript
 * const br = Br({ id: "line-break" })
 * ```
 */
export const Br = GlobalEmpty("Br")

export default Br
